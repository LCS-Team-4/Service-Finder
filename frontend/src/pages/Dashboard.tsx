import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { renderToStaticMarkup } from 'react-dom/server';
import { BookOpen, Bookmark, Bus, Clock3, Flame, GraduationCap, Heart, Hospital, House, Landmark, Library, LocateFixed, MapPinned, Minus, Navigation, Phone, Pill, Plus, Search, Shield, ShoppingBag, Smile, Stethoscope, X } from 'lucide-react';

declare const L: any;
type Category = 'Clinics' | 'Libraries' | 'Shelters' | 'Hospitals' | 'Police Stations' | 'Pharmacies' | 'Dentists' | 'SPCA' | 'Fire Stations' | 'Home Affairs' | 'Malls' | 'Transport' | 'Schools / Universities';
type Place = { name: string; category: Category; area: string; lat: number; lng: number };
const categories: { name: Category; symbol: string; color: string }[] = [{ name:'Clinics',symbol:'+',color:'#b94b3c'},{name:'Libraries',symbol:'▮',color:'#4f876f'},{name:'Shelters',symbol:'⌂',color:'#cb8c38'},{name:'Hospitals',symbol:'+',color:'#3b77a2'},{name:'Police Stations',symbol:'●',color:'#375f93'},{name:'Pharmacies',symbol:'●',color:'#81528d'},{name:'Dentists',symbol:'●',color:'#77909c'},{name:'SPCA',symbol:'♥',color:'#815c54'},{name:'Fire Stations',symbol:'♦',color:'#bd6240'},{name:'Home Affairs',symbol:'▦',color:'#75664b'},{name:'Malls',symbol:'●',color:'#a45b83'},{name:'Transport',symbol:'▰',color:'#847337'},{name:'Schools / Universities',symbol:'◆',color:'#43858a'}];
const places: Place[] = [
  ['Cape Town Civic Centre','Home Affairs','Cape Town',-33.925,18.424],['Groote Schuur Hospital','Hospitals','Observatory',-33.941,18.465],['Sea Point Police Station','Police Stations','Sea Point',-33.918,18.386],['Cape Town Central Library','Libraries','CBD',-33.925,18.423],['Woodstock Clinic','Clinics','Woodstock',-33.927,18.448],['Mowbray Maternity Hospital','Hospitals','Mowbray',-33.948,18.475],['Rondebosch Library','Libraries','Rondebosch',-33.961,18.476],['Khayelitsha Mall','Malls','Khayelitsha',-34.037,18.678],['Mitchells Plain Clinic','Clinics','Mitchells Plain',-34.048,18.617],['Bellville Police Station','Police Stations','Bellville',-33.900,18.628],['Tygerberg Hospital','Hospitals','Parow',-33.908,18.596],['Milnerton Library','Libraries','Milnerton',-33.879,18.496],['Hout Bay Fire Station','Fire Stations','Hout Bay',-34.044,18.348],['Wynberg SPCA','SPCA','Wynberg',-34.003,18.468],['Claremont Transport Hub','Transport','Claremont',-33.981,18.465],['UCT','Schools / Universities','Rondebosch',-33.957,18.461],['Table View Shelter','Shelters','Table View',-33.824,18.488],['Kloof Street Pharmacy','Pharmacies','Gardens',-33.932,18.410]
].map(([name,category,area,lat,lng]) => ({name:name as string,category:category as Category,area:area as string,lat:lat as number,lng:lng as number}));

const categoryIcon = (category: Category, size = 14) => { const props = { size, strokeWidth: 2.2 }; switch (category) { case 'Clinics': return <Stethoscope {...props}/>; case 'Libraries': return <Library {...props}/>; case 'Shelters': return <House {...props}/>; case 'Hospitals': return <Hospital {...props}/>; case 'Police Stations': return <Shield {...props}/>; case 'Pharmacies': return <Pill {...props}/>; case 'Dentists': return <Smile {...props}/>; case 'SPCA': return <Heart {...props}/>; case 'Fire Stations': return <Flame {...props}/>; case 'Home Affairs': return <Landmark {...props}/>; case 'Malls': return <ShoppingBag {...props}/>; case 'Transport': return <Bus {...props}/>; case 'Schools / Universities': return <GraduationCap {...props}/>; } };

function LeafletMap({ places, selected, onSelect, mapRef }: { places: Place[]; selected: Place | null; onSelect: (place: Place) => void; mapRef: React.MutableRefObject<any> }) {
  const root = useRef<HTMLDivElement>(null); const layer = useRef<any>(null);
  useEffect(() => { if (!root.current || !L) return; const map = L.map(root.current,{zoomControl:false,attributionControl:true}).setView([-33.96,18.50],11); L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map); mapRef.current=map; layer.current=L.layerGroup().addTo(map); return () => map.remove(); }, [mapRef]);
  useEffect(() => { if (!layer.current) return; layer.current.clearLayers(); places.forEach(place => { const item=categories.find(c=>c.name===place.category)!; const icon=L.divIcon({className:'cape-marker-wrap',html:`<div class="cape-marker" style="--marker:${item.color}"><span>${renderToStaticMarkup(categoryIcon(place.category, 14))}</span></div>`,iconSize:[34,42],iconAnchor:[17,42]}); const marker=L.marker([place.lat,place.lng],{icon}).addTo(layer.current); marker.bindTooltip(`<strong>${place.name}</strong><br>${place.category}`,{direction:'top',offset:[0,-38]}); marker.on('click',()=>onSelect(place)); }); },[places,selected,onSelect]);
  return <div className="leaflet-map" ref={root} />;
}

function Dashboard() { const [query,setQuery]=useState(''); const [active,setActive]=useState<Category|null>(null); const [selected,setSelected]=useState<Place|null>(null); const [notice,setNotice]=useState(''); const [aboutOpen,setAboutOpen]=useState(false); const [legendOpen,setLegendOpen]=useState(false); const [saved,setSaved]=useState<string[]>([]); const mapRef=useRef<any>(null); const visible=useMemo(()=>places.filter(p=>(!active||p.category===active)&&`${p.name} ${p.area} ${p.category}`.toLowerCase().includes(query.toLowerCase())),[active,query]); const selectPlace=useCallback((place:Place)=>{setSelected(place);mapRef.current?.flyTo([place.lat,place.lng],15,{animate:true,duration:.7})},[]); const search=()=>{const first=visible[0];if(first)selectPlace(first);setNotice(first?`${visible.length} place${visible.length===1?'':'s'} found`:'No places found')}; const locate=()=>navigator.geolocation?.getCurrentPosition(pos=>{mapRef.current?.flyTo([pos.coords.latitude,pos.coords.longitude],14);setNotice('Showing your current location.');},()=>setNotice('We could not access your location.')); const details=selected?{address:selected.category==='SPCA'?'1 Bird Street, Grassy Park':`${selected.area} service centre, Cape Town`,hours:selected.category==='Hospitals'?'Open 24 hours':'08:00 – 16:30',phone:selected.category==='SPCA'?'021 700 4158':'021 400 0000'}:null;
return <main className="guide-shell"><LeafletMap places={visible} selected={selected} onSelect={selectPlace} mapRef={mapRef}/><header className="masthead"><h1>The Cape Guide</h1><p>Find. Navigate. Connect.</p></header><section className="search-panel"><span className="glass"><Search size={19}/></span><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="Search for a place or service..."/><button className="search-button" onClick={search}>Search</button><button className="locate" title="Use my location" onClick={locate}><LocateFixed size={17}/></button></section>{notice&&<div className="notice">{notice}</div>}{selected&&details&&<section className="service-popup"><button className="service-close" aria-label="Close service" onClick={()=>setSelected(null)}><X size={18}/></button><div className="service-title"><span className="service-icon" style={{background:categories.find(c=>c.name===selected.category)?.color}}>{categoryIcon(selected.category,18)}</span><div><h2>{selected.name}</h2><p>{selected.category}</p></div></div><div className="service-info"><p><MapPinned size={17}/><span>{details.address}<br/>Cape Town</span></p><p><Clock3 size={17}/>{details.hours}</p><p><Phone size={17}/>{details.phone}</p></div><div className="service-actions"><button onClick={()=>setNotice(`More details for ${selected.name} coming soon.`)}>View Details</button><button className="directions" onClick={()=>window.open(`https://www.google.com/maps/dir/?api=1&destination=${selected.lat},${selected.lng}`,'_blank','noopener,noreferrer')}><Navigation size={14}/>Get Directions</button><button className={saved.includes(selected.name)?'saved':''} onClick={()=>setSaved(saved.includes(selected.name)?saved.filter(name=>name!==selected.name):[...saved,selected.name])}><Bookmark size={15}/>{saved.includes(selected.name)?'Saved':'Save'}</button></div></section>}<button className="panel-trigger about-trigger" onClick={()=>setAboutOpen(true)}><BookOpen size={17}/>About the Guide</button><button className="panel-trigger legend-trigger" onClick={()=>setLegendOpen(true)}><MapPinned size={17}/>Legend</button>{aboutOpen&&<aside className="about popup-panel"><button className="close-panel" aria-label="Close about" onClick={()=>setAboutOpen(false)}><X size={17}/></button><h2>About the Cape<br/>Guide</h2><p>The Cape Guide is a map-based service finder designed to help people discover useful public services across Cape Town. Search, explore and navigate to the services you need — all from one map.</p><div className="motto">Every road leads somewhere.<br/>Every service helps someone.</div></aside>}{legendOpen&&<aside className="legend popup-panel"><button className="close-panel" aria-label="Close legend" onClick={()=>setLegendOpen(false)}><X size={17}/></button><h2>Legend</h2>{categories.map(item=><button key={item.name} className={active===item.name?'active':''} onClick={()=>{setActive(active===item.name?null:item.name);setSelected(null)}}><i style={{background:item.color}}>{categoryIcon(item.name)}</i>{item.name}</button>)}<button className="you-are" onClick={locate}><i><MapPinned size={16}/></i>You Are Here</button></aside>}<div className="leaflet-zoom"><button aria-label="Zoom in" onClick={()=>mapRef.current?.zoomIn()}><Plus size={18}/></button><button aria-label="Zoom out" onClick={()=>mapRef.current?.zoomOut()}><Minus size={18}/></button></div></main> }

export default Dashboard;
