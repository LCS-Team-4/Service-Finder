import { Router } from 'express';
// import { getServices, suggestService } from '../controllers/serviceController';

const router = Router();

// router.get('/', getServices);
// router.post('/suggest', suggestService);

export default router;
